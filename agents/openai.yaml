interface:
  display_name: "UI Evidence-First Layers V3.0"
  short_description: "Evidence-led UI component layering and review"
  default_prompt: "Use $split-ui-design-layers-evidence-first-v3-0 to split this UI into full-canvas editable layers."
